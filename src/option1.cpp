#include <iostream>
#include <string>
#include <vector>
using namespace std;
class option1{
    private :
        hash<string> hash1;
        hash<string> hash2;
    public : 
        option1(hash<string> hash1,hash<string> hash2) : hash1(hash1) , hash2(hash2) {}
    
};